import React, { Fragment } from "react";
import { withRouter } from "react-router-dom";

const dummy = {
  mainPosts: [
    {
      User: {
        id: 1,
        nickname: "Jaemin"
      },
      content: "첫번째 게시글",
      img: "https://source.unsplash.com/random"
    }
  ]
};

const Post = () => {
  <Fragement>div.</Fragement>;
};

export default withRouter(Post);
